using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Todo: Record aspects of each level and put them in corresponding variables
// Todo: Keep track of time taken to beat each level as well as total time

public class DifficultyDeterminator : MonoBehaviour
{
    PlayerController Player;
    void Start()
    {
        
    }

    void Update()
    {
        // Every update check to see if a level has been beaten
    }

    void UpdateDeathCount(int deaths)
    {

    }
}
